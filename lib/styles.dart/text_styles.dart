import 'package:flutter/material.dart';

class AppStyles {
  static const titleStyle = TextStyle(
    fontSize: 23,
    color: Colors.white,
  );
  static const textGrey = TextStyle(fontSize: 23, color: Colors.grey);
  static const textWhite = TextStyle(
    fontSize: 50,
    fontWeight: FontWeight.bold,
    color: Colors.white,
  );
  static const labelStyle = TextStyle(
    fontSize: 20,
    color: Colors.grey,
  );
}
